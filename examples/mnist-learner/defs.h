#define WIDTH 280
#define HEIGHT 280

#define SIZE_IMAGE 784
#define WIDTH_IMAGE 28
#define HEIGHT_IMAGE 28

#define SCALING_FACTOR WIDTH / WIDTH_IMAGE

#define LEN(array) (sizeof((array)) / sizeof((array)[0]))